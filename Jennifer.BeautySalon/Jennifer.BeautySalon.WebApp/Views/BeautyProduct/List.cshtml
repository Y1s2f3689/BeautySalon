@using Jennifer.BeautySalon.Domain.Entities
@model IEnumerable<BeautyProduct>

@{
    ViewBag.Title = "BeautyProducts";
}

@foreach (var p in Model)
{
    <div>
        <h3>@p.Name</h3>
        @p.Description
        <h4>@p.Price.ToString("c")</h4>
    </div>
}

